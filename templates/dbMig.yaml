# harness.io/skip-file-for-deploy
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Values.name }}-job
spec:
  template:
    spec:
      containers:
      - name: pi
        image: python:3.6.12-alpine
        command: ['echo','hellworld']
      restartPolicy: Never
  backoffLimit: 4
